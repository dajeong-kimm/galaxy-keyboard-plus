{
  "name": "mcp-web-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder"
  },
  "dependencies": {
    "openai-agents-mcp": "^1.0.0",
    "electron": "^25.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "electron-builder": "^23.0.0"
  },
  "build": {
    "extraResources": [
      {
        "from": "resources/",
        "to": "resources/",
        "filter": [
          "*.exe"
        ]
      }
    ]
  }
}
